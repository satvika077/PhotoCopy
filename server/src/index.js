import express from 'express'; import cors from 'cors'; import helmet from 'helmet'; import dotenv from 'dotenv'; import { errorHandler } from './middleware/errorHandler.js'; import { authMiddleware } from './middleware/auth.js'; import payments from './routes/payments.js';
dotenv.config(); const app=express(); app.use(helmet()); app.use(cors({origin:process.env.FRONTEND_URL||'http://localhost:5173'})); app.use(express.json({limit:'1mb'})); app.get('/api/health',(req,res)=>res.json({ok:true,service:'quickprint-api'})); app.use('/api/payments',authMiddleware,payments); app.use(errorHandler); app.listen(process.env.PORT||5000,()=>console.log(`QuickPrint API listening on ${process.env.PORT||5000}`));
